version: 2

models:
  - name: fact_payment
    description: "payment data"
    columns:
      - name: payment_id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null

  - name: fact_rental
    description: "rental data"
    columns:
      - name: rental_id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null